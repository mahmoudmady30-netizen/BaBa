# رفع BaBa Kids على Google Play — دليل الخطوات

## ليه محتاج تعمل الخطوات دي بنفسك؟

ملف الـ AAB لازم يكون **موقّع بمفتاح توقيع (keystore)** خاص بيك إنت. المفتاح
ده زي هوية التطبيق على جوجل بلاي:

- لازم يتولّد على جهازك إنت (أو يتخزن كسر في GitHub Secrets) — **ماينفعش**
  حد تاني يولّده لك، ولا ينفع يتحط في الكود.
- **لو ضاع منك، مش هتقدر تنشر أي تحديث للتطبيق تاني أبدًا** على نفس الرابط.
  احفظ نسخة منه في مكان آمن (Google Drive خاص، أو مدير كلمات سر).

عشان كده الخطوات دي إنت اللي بتعملها، مش هي حاجة أقدر أعملها لك.

---

## الخطوة 1: ولّد مفتاح التوقيع (مرة واحدة بس في العمر)

على جهازك، افتح Terminal / Command Prompt واكتب:

```bash
keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias baba-upload
```

(الأمر `keytool` بيجي مع Java. لو مش موجود، نزّل JDK 17.)

هيسألك:
- **كلمة سر للـ keystore** — اكتبها واحفظها.
- اسمك، المدينة، الدولة... (معلومات عادية، مش لازم تكون دقيقة جدًا).
- **كلمة سر للمفتاح (key password)** — ممكن تخليها نفس الأولى.

الناتج: ملف اسمه `upload-keystore.jks`. **احفظه في مكان آمن ومتحطهوش في git.**

---

## الخطوة 2: حوّل المفتاح لنص عشان تحطه في GitHub

```bash
base64 -i upload-keystore.jks | tr -d '\n' > keystore-base64.txt
```

على ويندوز (PowerShell):
```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("upload-keystore.jks")) > keystore-base64.txt
```

افتح `keystore-base64.txt` وانسخ النص اللي جواه كله.

---

## الخطوة 3: حط الأسرار في GitHub

روح على الريبو بتاعك:
**Settings → Secrets and variables → Actions → New repository secret**

وضيف الأربعة دول:

| الاسم | القيمة |
|---|---|
| `KEYSTORE_BASE64` | النص اللي نسخته من الخطوة 2 |
| `KEYSTORE_PASSWORD` | كلمة سر الـ keystore |
| `KEY_ALIAS` | `baba-upload` |
| `KEY_PASSWORD` | كلمة سر المفتاح |

---

## الخطوة 4: ابني ملف الـ AAB

في GitHub، روح على تاب **Actions** → اختار **"Build AAB (Google Play)"** من
على الشمال → دوس **Run workflow**.

لما يخلص (حوالي 3-5 دقايق)، هتلاقي ملف `BaBaKids-release-aab` تحت
**Artifacts** في صفحة الـ run. نزّله وفك الضغط — جواه `app-release.aab`.

ده الملف اللي بترفعه على جوجل بلاي.

---

## الخطوة 5: قبل ما ترفع — حاجات مهمة

### رقم الإصدار
دلوقتي التطبيق `versionCode = 1` و `versionName = "1.0.0"` (في
`app/build.gradle.kts`). **كل مرة** ترفع تحديث جديد لجوجل بلاي، لازم تزوّد
`versionCode` بواحد (2، 3، 4...) وإلا جوجل هترفض الملف.

### حجم التطبيق
ملفات الصوت في التطبيق حوالي 60 ميجا، فالـ AAB هيطلع حوالي 40-50 ميجا بعد
الضغط. ده تحت حد جوجل (150 ميجا للوحدة الأساسية) فمفيش مشكلة.

### حاجات جوجل بلاي هتطلبها منك في Play Console
- **سياسة الخصوصية**: عندك `PRIVACY_POLICY.md` في المشروع — لازم ترفعه على
  لينك عام (GitHub Pages أو أي موقع) وتحط اللينك في Play Console.
- **قسم Data Safety**: هتملا استمارة عن البيانات اللي بتتجمع. تطبيقك بيشتغل
  أوفلاين بالكامل ومش بيجمع بيانات، فالإجابات هتكون بسيطة.
- **تطبيقات الأطفال**: التطبيق موجّه للأطفال، فهتحتاج تعدّي على قسم
  **"Families"** ومتطلباته (تصنيف المحتوى، عدم وجود إعلانات، إلخ). ده مهم
  وممكن ياخد وقت في المراجعة.
- **صور ولقطات شاشة**: أيقونة 512×512، صورة غلاف 1024×500، وعلى الأقل
  لقطتين شاشة من التطبيق.

---

## ملحوظة عن `isMinifyEnabled`

سايبها `false` عن قصد. تشغيل R8 (تصغير الكود) ممكن يشيل حاجات بيستخدمها
التطبيق وقت التشغيل بس ومش واضحة للأداة، وده بيسبب crashes مش بتظهر غير
بعد النشر. الفايدة هنا محدودة أصلاً (أغلب حجم التطبيق صوتيات مش كود)، فالأأمن
إنك تنشر أول إصدار من غيرها، وتجربها بعدين باختبار كويس لو حبيت.
